# Returns PROBABLE resolution — source-side pass (not a visual walk)

Home method applied. **Result: no open PROBABLE rows** — every Returns finding
already reached a terminal verdict; the summary's "PROBABLE: 1" was the legend line.

- **RT-01 refund-as-coin settlement ✅ RESOLVED** (#188); **RT-04 status history ✅**
  (#184); **RT-05 per-item reason → DEFER** (contract/codegen — flat single-reason
  contract; the read-path was confirmed); **RT-06 consumer status filter ✅ RESOLVED** (#189).
- **CONFIRMED-absent, terminal-DEFER (heavier features, separately tracked):**
  **RT-02** return cargo-leg (no return shipping/label/drop-off) and **RT-03** return
  photos (damage evidence) — both real gaps, but each is a feature vertical (cargo
  adapter / upload pipeline), not a source-side PROBABLE row to settle here.
- **NOT-ACTIONABLE (settled):** refund-as-coin (the wallet_credit model);
  status-derived timeline; brand tokens.

**Verdict:** Returns is **terminal for PROBABLE** — 0 CONFIRMED UI fixes, 0
NEEDS-VISUAL, 0 NEEDS-DECISION open. RT-02/RT-03 remain as engineering DEFERs (the
last substantive Returns features), and RT-05 as a contract/codegen DEFER.
