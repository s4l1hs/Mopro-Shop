# Account PROBABLE resolution — source-side pass (not a visual walk)

Home method: Mopro from code (fact) × Trendyol convention (provisional, ~May 2025,
*not visually verified*).

### AC-05 — no membership tier/points display → NOT-ACTIONABLE (intentional divergence) / NEEDS-DECISION
- **Mopro (fact):** the profile header shows `GET /me` (name) + **3 stat tiles —
  active orders, coin balance, active plans** (orders/wallet providers). No
  membership "tier/points" block.
- **Trendyol (provisional):** a membership level/points block (e.g. Elite) *(convention)*.
- **Verdict:** Mopro's **coin + plans** tiles are the deliberate analog to Trendyol's
  tier/points — a **settled business-model divergence** (the coin/cashback model;
  not re-opened per §5.3). The audit itself scopes AC-05 as "only if Mopro adopts a
  tier concept." → **NOT-ACTIONABLE** as a parity gap; adopting a tier system is a
  **NEEDS-DECISION (Salih)**, not a source fix.

## Outcome

| Row | Verdict |
|---|---|
| AC-05 membership tier/points | **NOT-ACTIONABLE** (coin+plans is the deliberate analog) · adopting a tier = **NEEDS-DECISION** |

**0 CONFIRMED fixes.** The Account NOT-ACTIONABLE set already records the
coin/cashback wallet hub + coin/plans stats as intentional. No NEEDS-VISUAL residue.

## Salih's residue (Account)
- **NEEDS-DECISION:** AC-05 — adopt a Trendyol-style membership tier, or keep the
  coin+plans model (current)? (Default: keep — it's the Mopro model.)
