# Secrets — NEVER commit
.env*
!.env.example

# Local data volumes (populated by docker compose)
/data/

# Temp build artefacts
/tmp/

# Go
/vendor/
*.test
coverage.out
*.out
*.coverprofile
profile.cov

# Binaries
*.exe
*.exe~
*.dll
*.so
*.dylib

# IDE
.DS_Store
.idea/
.vscode/

# go.work.sum (generated; go.work itself is committed — needed to override parent workspace)
go.work.sum

# Docker overrides
docker-compose.override.yml
